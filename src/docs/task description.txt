импортировать спо ручками.
сначала, т.е. можно импортировать файл в базу данных в новую таблицу(таблица создается, если ее не было). 
А потом написать скрипт по перегону информации в актуальные таблицы самотура.
